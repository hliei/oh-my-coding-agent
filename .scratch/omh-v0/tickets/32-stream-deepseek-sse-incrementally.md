# 32 — Stream DeepSeek SSE incrementally

**What to build:** Replace whole-response buffering at the private DeepSeek SSE boundary with incremental delivery through the existing public Models and Product Session seams. A complete text or Tool SSE event must become observable without waiting for HTTP EOF, and cancellation after a delivered text delta must settle a response whose transport is still active. Preserve strict parsing, terminal Usage, and Ticket 31's nested cleanup guarantees, then regenerate all Candidate-bound release evidence.

**Blocked by:** 31 — Shield nested DeepSeek cancellation cleanup.

**Status:** ready-for-agent

- [ ] A deterministic regression against the public installed-wheel seam delivers a complete text SSE event, holds the response open at a later read, and observes the corresponding text delta before releasing HTTP EOF; the current `await response.aread()` implementation fails this test.
- [ ] The shared Adapter consumes response bytes incrementally and forwards complete text and Tool events as they become available, without retaining the whole raw response until EOF or introducing a public transport/parser hook. Partial wire frames remain private until valid, and all four Models helpers preserve their existing request and terminal-result contracts.
- [ ] Equivalent valid SSE inputs split across network chunks, including UTF-8 code points, LF/CRLF event separators, and JSON payload boundaries, produce the same ordered public observations and terminal values. Partial frames are neither prematurely decoded nor silently dropped.
- [ ] Nullable ordinary-chunk Usage, zero-Usage partials, fragmented Tool Calls, the accepted terminal Usage layouts, and strict finalization retain their existing semantics. Missing, malformed, duplicate, inconsistent, truncated, and forbidden post-terminal payloads still fail closed; incremental delivery does not invent defaults or waive a terminal obligation.
- [ ] A barrier-driven Product Session regression observes a text delta while the response has not reached EOF and a later transport read is pending, then calls `session.abort()`. Cancellation stops and drains that operation, delivers exactly one aborted Assistant `MessageEnd` with the corresponding Run completion, reaches idle only after cleanup, and permits a fresh successful Run in the same Session. Buffered replay after HTTP EOF cannot satisfy this case.
- [ ] Ticket 31's response/client/transport/TLS-equivalent cleanup protection remains intact under later cancellation, repeated aborts, cancelled waiters, and disposal. Direct-helper cancellation preserves its original `CancelledError` after cleanup; managed cancellation retains its accepted terminal contract; genuine cleanup failure remains a distinct fail-closed lifecycle carrier rather than a forged successful cancellation.
- [ ] No automatic request retry, reconnect, or replacement operation is added, and public events, errors, and retained evidence expose no credential, authenticated request data, raw Provider response, or internal traceback. The caller-started post-settlement reuse Run remains distinct from a retry.
- [ ] Deterministic tests use causal event barriers and bounded watchdogs rather than timing sleeps or busy polling. Installed-wheel conformance exercises both pre-EOF incremental delivery and active-transport cancellation/reuse, with the required Matrix/corpus bindings; a source-only test or runner-only mock cannot substitute.
- [ ] Candidate commit `03433b1448f07a6427edd9dce40c000ff91a555f`, wheel SHA-256 `9b2b5a32650622e6604a684dc5fab8fa71c47f4659ce68d3b95d077e8676678d`, its build/Release Row results, and streaming evidence SHA-256 `3bf4af0e43df5a0146e165bbb71c1b6b158265eb608913edd685d143287f95ff` remain unauthorized and audit-only; none is reused for later release authorization.
- [ ] After the fix, a new reproducible Candidate Wheel is built and both macOS 26 arm64 CPython 3.12/3.13 Release Rows pass against its exact bytes and current authorities. Ticket 29 may then restart from its first live case only after its separately owned runner checks pass, using a newly issued human-owned credential, corrected frozen live contract, and entirely fresh live evidence.

## Comments

- 2026-08-27 — Created after the `03433b1` Candidate's human cancellation gate stopped at `cancel.terminal_count`. That immediate failure belongs to the assistant-authored Ticket 29 runner: Models terminal events are not Product Session `MessageUpdate` events. It is not the product defect owned here, and Ticket 31 remains resolved.
- 2026-08-27 — The independent installed-wheel diagnostic delivered a complete text SSE event while withholding HTTP EOF. It observed only `start` before EOF and `start,text_start,text_delta,text_end,done` after release. Source inspection confirmed `_iter_sse_payloads()` begins with `bytearray(await response.aread())`. This ticket owns incremental wire delivery and cancellation while the response is still active, not a change to the public event vocabulary or a waiver of strict terminal/cleanup checks.
- 2026-08-27 — Ticket 29 owns correction of its terminal observation and installed-Session runner tests before any fresh human attempt. The prior Candidate, frozen runner, attempt markers, and redacted streaming evidence are retained only for audit; the human confirmed key revocation and disposable-repository deletion. No implementation, live request, tag, or publication is performed by creating this ticket.
