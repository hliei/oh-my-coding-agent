# 31 — Shield nested DeepSeek cancellation cleanup

**What to build:** Repair the real DeepSeek cancellation cleanup path exposed by the failed Live Provider Gate. Once active-stream cancellation begins, every Provider resource owned by that operation must settle completely despite later cancellation delivery or repeated Product Session aborts; clean cancellation must retain the accepted aborted terminal and same-Session reuse, while a genuine cleanup failure remains a fail-closed lifecycle error.

**Blocked by:** 15 — Cancel, fail, reuse, and dispose a Product Session.

**Status:** resolved

- [x] A deterministic public-seam regression reproduces active cancellation while nested DeepSeek response, client, transport, and TLS-equivalent cleanup is still pending; it must not reduce the scenario to one synthetic close barrier.
- [x] Cancellation becomes observable before cleanup begins, and every resource owned by the Model operation is closed and drained exactly once before the operation settles.
- [x] Later cancellation delivery, repeated `abort()`, cancelled waiters, and concurrent `waitForIdle()` or `dispose()` calls cannot interrupt, duplicate, or abandon the shared cleanup attempt.
- [x] Successful cleanup produces exactly one aborted terminal, no `LifecycleError`, complete idle settlement, and a fresh successful Run in the same Product Session.
- [x] A genuine resource-close failure still produces `LifecycleError(code="cleanup")`, never forges an aborted terminal or idle/reusable state, and remains retryable only where the existing disposal contract explicitly permits cleanup retry.
- [x] Direct `streamSimple` and `completeSimple` cancellation still re-raises the caller's original `CancelledError` only after cleanup, while owner-managed `stream` and `complete` retain their single aborted-terminal contract.
- [x] The fix performs no automatic request retry or reconnect, starts no replacement Model operation, and exposes no credential, authenticated request bytes, raw Provider response, natural-language content, or internal cleanup traceback through public events or errors.
- [x] Barrier-driven tests cover cancellation before, during, and after each cleanup cutoff without timing sleeps or busy polling, and installed-wheel conformance exercises the repaired public Product Session path.
- [x] Candidate commit `079ae559d314ce09a32f4b4d20d2a7e711bea234`, wheel SHA-256 `308e435fbb663db415a978411a5139699ad68e3fcd9265f29b2033735c737260`, its Release Rows, and streaming evidence SHA-256 `040d771437ca6d3c0d4c12fb0ecc46d6f7871c109a77ad26dd910b64a3e2309c` remain permanently unauthorized and cannot be reused.
- [x] After the fix, a new reproducible Candidate Wheel is built and both macOS 26 arm64 CPython 3.12/3.13 Release Rows pass against its exact bytes before Ticket 29 restarts from its first live case with a newly issued credential and fresh evidence.

## Comments

- 2026-08-27 — Created from the second failed Ticket 29 Live Provider Gate. The deterministic cancellation coverage accepted by Tickets 11 and 15 controls a single synthetic close barrier, but the real Candidate reached nested HTTPX/httpcore/AnyIO TLS cleanup where cancellation interrupted resource close and surfaced `LifecycleError("DeepSeek cleanup failed")`. The historical tickets remain resolved; this ticket owns the newly observed public-seam regression and complete downstream Candidate invalidation.

- 2026-08-27 — Public-seam TDD reproduced interrupted nested response/client/transport/TLS-equivalent cleanup, including HTTPX automatic response close at EOF. The Provider now owns and shields each complete close attempt against later cancellation while preserving the first direct caller `CancelledError`; owner-managed cancellation emits exactly one aborted terminal only after settlement. Real close failure remains `LifecycleError(code="cleanup")`; Product Session admission stays closed even when prompt cancellation carries that cleanup failure at owner settlement. No retry, reconnect, replacement operation, or public secret/traceback exposure was added.
- 2026-08-27 — Sixteen Ticket 31 barrier-driven regressions cover first and later cancellation at started/drained cutoffs, entered cancelled abort/idle waiters, concurrent disposal, original cancellation identity, genuine close failure, and same-Session success after clean cancellation. The shared pytest-free public Product Session scenario also passes against the installed wheel and is bound as local-release obligation `omh-v0.deepseek-nested-cancellation-cleanup`; no private product test hook or fixed-Reference observation was added. Final locked suite: 580 passed; strict mypy: 94 source files clean; compileall, lock validation, and `git diff --check` passed. Standards and Spec implementation reviews against `75bbdca2ef0f5039aaa932ea1d86b30c5ef7d9b0` reported zero remaining findings.
- 2026-08-27 — Fresh independent fixed-Reference captures from revision `0e6909f050eeb15e8f6c05185511f3788357ddb3` were byte-identical to the updated corpus SHA-256 `d336c7efe818dd1102b757f16ffb47590aa6ec3f4643bd03292b21badacfaed6`. Matrix SHA-256: `6da4991c1d5ce04aba76fda01481fc0d14ff1988bc4fc6a952cde09823f311fd`; lock SHA-256: `767fe838b141aabe74107469c96e62a256513533c043d647279c01c1d162ef41`.
- 2026-08-27 — Two isolated builds from Candidate commit `03433b1448f07a6427edd9dce40c000ff91a555f` produced byte-identical `omh-0.1.0-py3-none-any.whl`, SHA-256 `9b2b5a32650622e6604a684dc5fab8fa71c47f4659ce68d3b95d077e8676678d`. macOS 26.3.1 (25D771280a) arm64 CPython 3.12.13 and 3.13.8 each installed those exact bytes offline from a row-specific locked only-binary wheelhouse outside the checkout and passed the complete installed suite with platform evidence; neither `--skip-suite` nor `--platform-only` was used. Both fresh row results bind successfully with `readyForReleaseEvidenceBundle: true` and `publishRight: false`.
- 2026-08-27 — Candidate artifacts and captured JSON evidence are retained under `/private/tmp/omh-ticket31.WkFsMY/`: wheel at `build/candidate/omh-0.1.0-py3-none-any.whl`; `build-evidence.json` SHA-256 `f0d95c4beb9d541d52a6e90636acd43e21b034385374dfa1296e8939f6548daf`; `macos-312.json` SHA-256 `9d53c8da5bb0ae3526942c6612250867de8bab98f05bcdbb2f3e76a22fa86055`; `macos-313.json` SHA-256 `93b2a625244254f09720912a17c6e5c6383fd88ad856598da427080fb63137d8`; `bound-release-rows.json` SHA-256 `a4af0d30751ebc92d8615b62a9a3ea4c9efd1cd4c976da65d6b6948eaea717b1`. These temporary artifacts must remain available until the human gate captures its immutable bundle.
- 2026-08-27 — Candidate `079ae559d314ce09a32f4b4d20d2a7e711bea234`, wheel `308e435fbb663db415a978411a5139699ad68e3fcd9265f29b2033735c737260`, its Release Rows, and streaming evidence `040d771437ca6d3c0d4c12fb0ecc46d6f7871c109a77ad26dd910b64a3e2309c` remain permanently unauthorized; none was reused. Ticket 29 can now restart from its first live case only for the new exact Candidate, using a newly issued human-owned credential and fresh evidence. No live case, credential access, tag, or publication was performed by this ticket.
