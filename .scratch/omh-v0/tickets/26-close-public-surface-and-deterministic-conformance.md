# 26 — Close the Public Import Surface and deterministic conformance

**What to build:** Close the complete deterministic v0 contract over the installed Distribution. The final public allowlists, every selected journey and platform-independent behavior, all 59 active Parity Ledger keys plus the six non-reusable Session tombstones, the fixed Reference Observation Corpus, and the Conformance Obligation Matrix form one fail-closed graph with no missing, duplicate, mismatched, orphaned, or implementation-only evidence.

**Blocked by:** 21 — Load a Python Extension with lifecycle and Tool contribution; 25 — Settle Command signals, recovery, and shutdown.

**Status:** ready-for-agent

- [ ] Installed metadata, all three import roots/child paths, exact names/members/signatures including `SessionManager`, version-3 Session carriers, `AgentSession` projections/compaction controls, four low-level loops, and absence of every excluded re-export/alias match the closed Public Import Surface.
- [ ] Every applicable Verification clause from the 13 resolved decisions has one or more executable cases driven only through installed public imports or Command Modes.
- [ ] The Matrix contains every public member, required product/companion journey, PA, ABD, dependency/packaging obligation, and selected platform obligation exactly once under a valid authority/evidence class.
- [ ] All 11 Python Adaptation and 48 active Accepted Behavioral Deviation keys match their complete decision-owned Ledger records; the six withdrawn Session keys remain tombstones and cannot appear as active authority, normalization, or executable expectation.
- [ ] Exact-parity and PA/ABD cases link fixed-Reference citations and corpus observations; local-release cases use only the accepted explicit `not_applicable` reasons.
- [ ] The Reference Observation Corpus is reproducibly regenerated byte-for-byte from the fixed revision and never records current omh output as its oracle.
- [ ] Deterministic tests control time, entropy, UUID, transport, and injected failure only through private seams while asserting public results/events/errors/bytes or real isolated effects.
- [ ] Real JSONL filesystem, in-memory manager, concurrent-manager-without-lease, subprocess-tree, signal, and terminal obligations use actual resources; concurrency uses explicit barriers with no sleeps, retry, skip, xfail, or order dependence.
- [ ] A bounded watchdog can only fail a hang and never change product semantics; the complete suite performs no external network request.
- [ ] The closure audit reports zero known or unresolved Parity Gaps, zero uncovered obligations, and zero orphan cases and passes every locked quality check.
