# 26 — Close the Public Import Surface and deterministic conformance

**What to build:** Close the complete deterministic v0 contract over the installed Distribution. The final public allowlists, every selected journey and platform-independent behavior, all 59 active Parity Ledger keys plus the six non-reusable Session tombstones, the fixed Reference Observation Corpus, and the Conformance Obligation Matrix form one fail-closed graph with no missing, duplicate, mismatched, orphaned, or implementation-only evidence.

**Blocked by:** 21 — Load a Python Extension with lifecycle and Tool contribution; 25 — Settle Command signals, recovery, and shutdown.

**Status:** resolved

- [x] Installed metadata, all three import roots/child paths, exact names/members/signatures including `SessionManager`, version-3 Session carriers, `AgentSession` projections/compaction controls, four low-level loops, and absence of every excluded re-export/alias match the closed Public Import Surface.
- [x] Every applicable Verification clause from the 13 resolved decisions has one or more executable cases driven only through installed public imports or Command Modes.
- [x] The Matrix contains every public member, required product/companion journey, PA, ABD, dependency/packaging obligation, and selected platform obligation exactly once under a valid authority/evidence class.
- [x] All 11 Python Adaptation and 48 active Accepted Behavioral Deviation keys match their complete decision-owned Ledger records; the six withdrawn Session keys remain tombstones and cannot appear as active authority, normalization, or executable expectation.
- [x] Exact-parity and PA/ABD cases link fixed-Reference citations and corpus observations; local-release cases use only the accepted explicit `not_applicable` reasons.
- [x] The Reference Observation Corpus is reproducibly regenerated byte-for-byte from the fixed revision and never records current omh output as its oracle.
- [x] Deterministic tests control time, entropy, UUID, transport, and injected failure only through private seams while asserting public results/events/errors/bytes or real isolated effects.
- [x] Real JSONL filesystem, in-memory manager, concurrent-manager-without-lease, subprocess-tree, signal, and terminal obligations use actual resources; concurrency uses explicit barriers with no sleeps, retry, skip, xfail, or order dependence.
- [x] A bounded watchdog can only fail a hang and never change product semantics; the complete suite performs no external network request.
- [x] The closure audit reports zero known or unresolved Parity Gaps, zero uncovered obligations, and zero orphan cases and passes every locked quality check.

## Comments

- Closed the installed public surface with exact root, child-module, member, signature, annotation, metadata, and excluded-alias inventories; every Matrix obligation now owns one unique corpus/capture case and one or more independently indexed executable runners.
- Expanded installed-wheel evidence across public values, managed cancellation, Session construction, Python Extensions, the programmatic/one-shot journeys, real PTY REPL transcripts, Command signals, and the complete surface. Deterministic concurrency uses explicit barriers and bounded watchdogs with no sleeps, retries, skips, or xfails.
- Removed all fixed observations from the capture manifest. The Reference Capture Row validates the complete cited fixed-source snapshot, gates test-cited behavior through selected upstream/custom tests, requires all 99 fixed cases in the independent capture output, and rejects static, missing, duplicate, or orphan observations.
- Reference verification used two independent clean `git archive` exports at revision `0e6909f050eeb15e8f6c05185511f3788357ddb3`, official Node 22.19.0 binary SHA-256 `0d005c18e095027ca8f9fe1cc1126f767ee4ad7b004f4d6fb781ec4228a7c5d1`, bundled npm 10.9.3, offline install, process-level network denial, and bounded commands. Both captures matched corpus SHA-256 `ea9b943f190bb493314450c16182dc7443b21def6f15e5f354f36e26a1a17fe5` byte for byte.
- Verification: full locked suite passed (528 tests); locked mypy passed for 93 source files; compileall, lock validation, and `git diff --check` passed. Standards and Spec fixed-point reviews were clean.
