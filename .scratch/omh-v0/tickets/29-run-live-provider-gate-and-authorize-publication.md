# 29 — Run the Live Provider Gate and authorize publication

**What to build:** Perform the human-owned final release gate for the exact Candidate Wheel. A named human knowingly injects a newly issued revocable real DeepSeek credential on the selected host, reviews three live cases and secret handling, revokes the credential, completes the immutable Release Evidence Bundle, and grants the Publish Right only for the already verified wheel through the bound annotated tag.

**Blocked by:** 28 — Prove both Release Rows; 30 — Accept nullable DeepSeek streaming Usage; 31 — Shield nested DeepSeek cancellation cleanup; 32 — Stream DeepSeek SSE incrementally.

**Status:** ready-for-human

- [ ] The gate runs on the accepted macOS 26 arm64 host with isolated CPython 3.12, the exact Candidate Wheel, and its locked wheelhouse after all deterministic Release Rows pass.
- [ ] A named human issues and temporarily injects a new `DEEPSEEK_API_KEY`, acknowledges that built-in shell execution has ordinary host/environment authority, and keeps unrelated secrets/resources outside the controlled fixture.
- [ ] One live non-thinking no-Tool stream proves ordered public events, normal terminal classification, and complete valid Usage without asserting natural-language bytes.
- [ ] One live active-stream cancellation settles completely and is followed by a fresh successful Run in the same Product Session.
- [ ] One live programmatic Product Journey uses `projectTrusted=False` in a disposable repository to inspect, mutate, complete Tool round trips, run a specifically named successful check after the final mutation, and report truthfully.
- [ ] Each Model operation makes no automatic retry; Provider/account/rate/service failure blocks release and cannot be replaced by Faux, deterministic fixtures, or prior live evidence.
- [ ] The credential is absent from every event, Tool output, transcript, error, log, persistent JSONL or in-memory Session entry, raw retained response, and evidence record; any disclosure fails the gate.
- [ ] The named human revokes the credential, deletes the disposable repository, and signs the redacted candidate-bound live evidence.
- [ ] The Release Evidence Bundle binds candidate commit/version, exact wheel filename/hash, lock, Matrix/corpus hashes, reproducible build, both Release Rows, live evidence, zero unresolved gaps/orphans, and named approval.
- [ ] After successful review, the named human creates the bound annotated release tag and publication uploads only the already verified wheel; any changed commit, wheel, lock, authority, contract, or evidence requires the complete gate again.

## Comments

- 2026-08-27 — Named human `hliei` began the gate for Candidate commit `c7ca2c7f7ce4ba30d19989e8099c36ef2bdd7e0b`, wheel `omh-0.1.0-py3-none-any.whl`, SHA-256 `13254467a3787519f7e5343801de642da83689b0adb98aa1381b3a9e39c71124`, on macOS 26.3.1 (25D771280a) arm64 with isolated CPython 3.12.13. The first non-thinking no-Tool streaming attempt failed closed with an initially over-redacted `GateFailure`; its newly issued key was revoked. A second complete attempt with a different newly issued key reached public `start` but no `done` and failed `stream.done_missing`; that key was also revoked. No automatic retry occurred.
- 2026-08-27 — The failed live shape exposed a release-blocking deterministic gap now owned by Ticket 30: current DeepSeek Chat Completions streams may include explicit `usage: null` on ordinary chunks, while the Candidate rejects any present non-mapping `usage` immediately after `start`; existing fixtures omit ordinary-chunk Usage instead of representing the official nullable field. Both attempts produced zero live evidence, the disposable repository was clean and moved to Trash, the repository remained clean, annotated tag `v0.1.0` was absent, and bound Release Row evidence retained `publishRight: false`. This Candidate and all of its prior build/row evidence remain unauthorized and cannot resume Ticket 29.
- 2026-08-27 — Named human `hliei` restarted the gate with a newly issued credential for Candidate commit `079ae559d314ce09a32f4b4d20d2a7e711bea234`, wheel `omh-0.1.0-py3-none-any.whl`, SHA-256 `308e435fbb663db415a978411a5139699ad68e3fcd9265f29b2033735c737260`. The live non-thinking no-Tool stream passed and produced candidate-bound evidence SHA-256 `040d771437ca6d3c0d4c12fb0ecc46d6f7871c109a77ad26dd910b64a3e2309c`, but the required active-stream cancellation then failed closed with `failure_code=runtime_or_provider_failure`. The retained diagnostic showed `LifecycleError("DeepSeek cleanup failed")` while real HTTPX/httpcore/AnyIO TLS cleanup was being interrupted by cancellation. No automatic retry occurred, and the retained output contained no credential, raw Provider response, or natural-language content.
- 2026-08-27 — The cancellation failure is a new release blocker owned by Ticket 31. The live runner also failed to retrieve the prompt task after `session.abort()` raised and emitted a full `Task exception was never retrieved` traceback; every future Ticket 29 runner must retrieve each created task on every exit path and emit only one stable redacted failure record. The Candidate credential was revoked and the disposable repository was deleted. No cancellation or Product Journey evidence was accepted, annotated tag `v0.1.0` remains absent, and `publishRight` remains false. Candidate `079ae559d314ce09a32f4b4d20d2a7e711bea234`, its wheel, its Release Rows, and the otherwise passing streaming evidence are permanently unauthorized and cannot be reused by a later Candidate or Live Provider Gate.
- 2026-08-27 — Ticket 31 is resolved with new Candidate commit `03433b1448f07a6427edd9dce40c000ff91a555f`, wheel `omh-0.1.0-py3-none-any.whl`, SHA-256 `9b2b5a32650622e6604a684dc5fab8fa71c47f4659ce68d3b95d077e8676678d`. Its reproducible build and both complete installed macOS 26 arm64 CPython 3.12/3.13 Release Rows pass; fresh bound evidence is `/private/tmp/omh-ticket31.WkFsMY/bound-release-rows.json`, SHA-256 `a4af0d30751ebc92d8615b62a9a3ea4c9efd1cd4c976da65d6b6948eaea717b1`, with artifact paths and individual hashes recorded in Ticket 31. The human gate must restart from the first live case with a newly issued credential and fresh evidence; task retrieval and redacted failure handling remain mandatory. All old Candidate and live evidence remain unauthorized. No live case has run for this Candidate, all acceptance boxes here remain open, and `publishRight` remains false.
- 2026-08-27 — Named human `hliei` ran the new gate from `/Users/andyli/Workspace/omh-v0.1.0-release-03433b1` with a newly issued credential. The streaming runner reported success with ordered `start`, `text_start`, `text_delta`, `text_end`, `done`, normal `stop`, and Usage of 26 input plus 1 output token. Its redacted `evidence/live-streaming.json` SHA-256 is `3bf4af0e43df5a0146e165bbb71c1b6b158265eb608913edd685d143287f95ff`, bound to the Candidate and live runner SHA-256 `8fc836247dc64341a8d0045ba0441943fa85133df9b4d736c3107c81cb8aa1af`; approval remained pending. Cancellation then stopped with the single redacted record `LIVE_GATE_FAILED case=cancellation failure_code=cancel.terminal_count`. Execution had passed `session.abort()`, prompt settlement, and the retained Assistant's `stopReason="aborted"` check, but stopped before the idle assertion and fresh same-Session Run. No cancellation success evidence or Product Journey evidence was produced.
- 2026-08-27 — The immediate failure was an incorrect assertion added by the assistant to the human runner, not evidence of a repeated product terminal or a Ticket 31 cleanup regression. It required one `message_update:error`, although Core consumes the Models `done`/`error` events as terminal Assistant values and projects them through `MessageEnd`; they are not forwarded as `MessageUpdate`. Two offline reproductions against the installed Candidate produced one aborted Assistant `MessageEnd` and zero `message_update:error` events, triggering the same failure code. An in-memory diagnostic substitution of the correct observation completed same-Session reuse twice; these deterministic diagnostics do not count as live evidence. The earlier eight runner-only checks covered task retrieval and redaction, not this normal cancellation path. The live runner and evidence were left unchanged.
- 2026-08-27 — Diagnosis also exposed the independent product defect now owned by Ticket 32: `_iter_sse_payloads()` calls `await response.aread()` before parsing SSE. A controlled installed-wheel transport delivered a complete text SSE event and withheld HTTP EOF; only `start` was observable until EOF was released, after which the text events and terminal arrived. Thus cancelling after a text delta on this Candidate cannot establish cancellation of a still-active HTTP response. Ticket 31 remains resolved; its nested cleanup fix is preserved, while the missing incremental delivery and pre-EOF cancellation coverage are a new release blocker. The passing streaming record above proves event order and terminal Usage, not pre-EOF delivery.
- 2026-08-27 — Before the next human gate, the Ticket 29 runner must observe the public Product Session seam: count exactly one aborted Assistant `MessageEnd` for the cancelled Run, verify Run completion and complete idle settlement, then require a fresh successful Run in the same Session. It must not infer the terminal from `message_update:error` or `message_update:done`. Against the newly installed Candidate, offline tests must cover this normal path, reject duplicate terminals and premature idle/reuse, and cover the abort/disposal failure paths with complete task retrieval and one stable redacted diagnostic. Synthetic runner-only mocks do not replace the installed Product Session path, and deterministic checks do not replace any live case. Freeze the corrected runner before starting the fresh gate; do not edit the currently bound runner or resume its attempted cancellation case.
- 2026-08-27 — The human confirmed revocation of the `03433b1` Candidate key and deletion of its disposable repository; the original fixture path is absent on recheck. The main repository and retained staging evidence remained unchanged, annotated tag `v0.1.0` is absent, and bound evidence retains `publishRight: false`. Candidate commit `03433b1448f07a6427edd9dce40c000ff91a555f`, wheel SHA-256 `9b2b5a32650622e6604a684dc5fab8fa71c47f4659ce68d3b95d077e8676678d`, its build/Release Row results, and streaming evidence `3bf4af0e43df5a0146e165bbb71c1b6b158265eb608913edd685d143287f95ff` remain unauthorized and audit-only; none may substitute for the new Candidate, fresh rows, or fresh live evidence required after Ticket 32. No failure is waived and no live retry or publication was performed.
